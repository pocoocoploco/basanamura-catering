{
  "name": "batak-catering-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-cost catering landing page with local JSON data and a small Node backend for local inquiries.",
  "scripts": {
    "build": "node scripts/build-static.js",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
